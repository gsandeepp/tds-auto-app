# Example .env file. Do NOT store real tokens in version control.
# Copy to .env and populate with real values for local development.

MY_SECRET=MY_SECRET = YOLO
GITHUB_TOKEN=
GITHUB_USERNAME=
OPENAI_API_KEY=
